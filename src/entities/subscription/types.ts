// Subscription entity type definitions — sourced from shared/api to avoid duplication.
export type {
  Plan,
  SubscriptionResponse,
  EntitlementsResponse,
  RefundResponse,
} from "@/shared/api/billing";
