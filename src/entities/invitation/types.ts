// Invitation entity type definitions — sourced from shared/api to avoid duplication.
export type {
  InvitationStatus,
  InvitationAuthority,
  Invitation,
  InvitationPreview,
} from "@/shared/api/iam";
