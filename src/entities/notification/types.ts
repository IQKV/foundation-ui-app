// Notification entity type definitions — sourced from shared/api to avoid duplication.
export type {
  UserNotification,
  UserNotificationListResponse,
  UnreadCountResponse,
} from "@/shared/api/iam";
